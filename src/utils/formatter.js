import chalk from 'chalk';\nimport { table } from 'table';\n\nexport function printSuccess(message) {\n  console.log(chalk.green('✓ ' + message));\n}\n\nexport function printError(message) {\n  console.error(chalk.red('✗ ' + message));\n}\n\nexport function printInfo(message) {\n  console.log(chalk.blue('ℹ ' + message));\n}\n\nexport function printWarning(message) {\n  console.log(chalk.yellow('⚠ ' + message));\n}\n\nexport function printCard(card) {\n  console.log(chalk.cyan('\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━'));\n  console.log(chalk.bold('Transaction Card Details'));\n  console.log(chalk.cyan('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━'));\n  console.log(`${chalk.bold('ID:')} ${card.id}`);\n  console.log(`${chalk.bold('Amount:')} $${parseFloat(card.amount).toFixed(2)}`);\n  console.log(`${chalk.bold('Date:')} ${card.date}`);\n  console.log(`${chalk.bold('Description:')} ${card.description}`);\n  console.log(`${chalk.bold('Category:')} ${card.category}`);\n  console.log(`${chalk.bold('Status:')} ${getStatusColor(card.status)(card.status)}`);\n  console.log(chalk.cyan('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\\n'));\n}\n\nexport function printCardsTable(cards) {\n  if (cards.length === 0) {\n    printWarning('No transaction cards found.');\n    return;\n  }\n\n  const data = [\n    [chalk.bold('ID'), chalk.bold('Amount'), chalk.bold('Date'), chalk.bold('Description'), chalk.bold('Category'), chalk.bold('Status')],\n    ...cards.map(card => [\n      card.id,\n      `$${parseFloat(card.amount).toFixed(2)}`,\n      card.date,\n      card.description.length > 20 ? card.description.substring(0, 20) + '...' : card.description,\n      card.category,\n      getStatusColor(card.status)(card.status)\n    ])\n  ];\n\n  console.log();\n  console.log(table(data, {\n    border: {\n      topBody: '─',\n      topJoin: '┬',\n      topLeft: '┌',\n      topRight: '┐',\n      bottomBody: '─',\n      bottomJoin: '┴',\n      bottomLeft: '└',\n      bottomRight: '┘',\n      bodyLeft: '│',\n      bodyRight: '│',\n      bodyJoin: '│',\n      joinBody: '─',\n      joinLeft: '├',\n      joinRight: '┤',\n      join: '┼'\n    }\n  }));\n}\n\nfunction getStatusColor(status) {\n  switch (status.toLowerCase()) {\n    case 'completed':\n      return chalk.green;\n    case 'pending':\n      return chalk.yellow;\n    case 'failed':\n      return chalk.red;\n    case 'cancelled':\n      return chalk.gray;\n    default:\n      return chalk.white;\n  }\n}\n